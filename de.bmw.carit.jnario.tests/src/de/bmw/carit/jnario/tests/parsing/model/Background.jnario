Feature: A new feature.
Background:
	Given a user name.
		var username = "PowerUser"
	And a password.
		var password = "password"
	And I am logged in.
Scenario: A fancy scenario.
	When I do something fancy
	Then it should work
Scenario: A not so fancy scenario.
	When I do something not so fancy
	Then it should still work